[package]
name = "shared-contract"
version = "0.1.0"
edition = "2024"
publish = false
description = "Shared IOCTL constants and limits for {{ project-name }} (no_std; kernel + user mode)"

[dependencies]
